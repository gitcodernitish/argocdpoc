`apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: repository.rubrik.com/rkdocker/sd_dev:f3b49cf50bb96783cf7c5fce348cbd34057843da
        command:
          - /bin/sh
          - echo "this is nitish"
        ports:
        - containerPort: 8080
      imagePullSecrets:
      - name: gcr-creds
`
